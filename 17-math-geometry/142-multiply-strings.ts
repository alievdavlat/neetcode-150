/**
 * 142. Multiply Strings   ·   Medium   ·   Math & Geometry
 *
 * Given two non-negative integers as strings, return their product as a
 * string. You may not convert the inputs to a number type or use a big-integer
 * library.
 *
 * Example 1:
 *   Input:  num1 = "2", num2 = "3"
 *   Output: "6"
 *
 * Example 2:
 *   Input:  num1 = "123", num2 = "456"
 *   Output: "56088"
 *
 * Constraints:
 *   - 1 <= num1.length, num2.length <= 200
 *   - Both consist of digits only and have no leading zeros except "0" itself
 *
 * Follow-up: digits[i + j + 1] is where num1[i] * num2[j] lands. Convince
 * yourself before coding.
 *
 * Pattern:   Schoolbook multiplication into a digit array
 * Target:    O(n * m) time, O(n + m) space
 * LeetCode:  https://leetcode.com/problems/multiply-strings/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=132745s  (36:52:25)
 */
export function multiply(num1: string, num2: string): string {
  throw new Error('Not implemented');
}
