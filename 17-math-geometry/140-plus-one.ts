/**
 * 140. Plus One   ·   Easy   ·   Math & Geometry
 *
 * digits holds the decimal digits of a large integer, most significant first,
 * with no leading zeros. Add one to the number and return the resulting digit
 * array.
 *
 * Example 1:
 *   Input:  digits = [1, 2, 3]
 *   Output: [1, 2, 4]
 *
 * Example 2:
 *   Input:  digits = [4, 3, 2, 1]
 *   Output: [4, 3, 2, 2]
 *
 * Example 3:
 *   Input:  digits = [9, 9]
 *   Output: [1, 0, 0]
 *
 * Constraints:
 *   - 1 <= digits.length <= 100
 *   - 0 <= digits[i] <= 9 with no leading zeros
 *
 * Follow-up: Only one input shape grows the array. Which one?
 *
 * Pattern:   Propagate the carry from the last digit backwards
 * Target:    O(n) time, O(1) extra space
 * LeetCode:  https://leetcode.com/problems/plus-one/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=131853s  (36:37:33)
 */
export function plusOne(digits: number[]): number[] {
  throw new Error('Not implemented');
}
