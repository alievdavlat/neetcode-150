/**
 * 1861. Find all anagrams of a string in a   ·   Easy   ·   Everyday Functions
 *
 * Write a function to find all anagrams of a string in a given list of strings
 * using lambda function.
 *
 * Example 1:
 *   Input:  words = ["bcda","abce","cbda","cbea","adcb"], text = "abcd"
 *   Output: ["bcda","cbda","adcb"]
 *
 * Example 2:
 *   Input:  words = ["recitals"," python"], text = "articles"
 *   Output: ["recitals"]
 *
 * Example 3:
 *   Input:  words = [" keep"," abcdef"," xyz"], text = " peek"
 *   Output: [" keep"]
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function anagramLambda(words: string[], text: string): string[] {
  throw new Error('Not implemented');
}
