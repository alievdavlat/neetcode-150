/**
 * 1396. Check whether the given string starts and ends   ·   Easy   ·   Everyday Functions
 *
 * Write a function to check whether the given string starts and ends with the
 * same character or not using regex.
 *
 * Example 1:
 *   Input:  text = "abba"
 *   Output: "Valid"
 *
 * Example 2:
 *   Input:  text = "a"
 *   Output: "Valid"
 *
 * Example 3:
 *   Input:  text = "abcd"
 *   Output: "Invalid"
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function checkChar(text: string): string {
  throw new Error('Not implemented');
}
