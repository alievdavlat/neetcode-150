/**
 * 1640. Remove the parenthesis area in a string   ·   Easy   ·   Everyday Functions
 *
 * Write a function to remove the parenthesis area in a string.
 *
 * Example 1:
 *   Input:  words = ["python (chrome)"]
 *   Output: ["python"]
 *
 * Example 2:
 *   Input:  words = ["string(.abc)"]
 *   Output: ["string"]
 *
 * Example 3:
 *   Input:  words = ["alpha(num)"]
 *   Output: ["alpha"]
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function removeParenthesis(words: string[]): string[] {
  throw new Error('Not implemented');
}
