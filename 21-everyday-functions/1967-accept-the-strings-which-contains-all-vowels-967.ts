/**
 * 1967. Accept the strings which contains all vowels   ·   Easy   ·   Everyday Functions
 *
 * Write a function to accept the strings which contains all vowels.
 *
 * Example 1:
 *   Input:  text = "SEEquoiaL"
 *   Output: "accepted"
 *
 * Example 2:
 *   Input:  text = "program"
 *   Output: "not accepted"
 *
 * Example 3:
 *   Input:  text = "fine"
 *   Output: "not accepted"
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function check(text: string): string {
  throw new Error('Not implemented');
}
