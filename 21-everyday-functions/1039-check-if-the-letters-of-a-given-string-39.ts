/**
 * 1039. Check if the letters of a given string   ·   Easy   ·   Everyday Functions
 *
 * Write a function to check if the letters of a given string can be rearranged
 * so that two characters that are adjacent to each other are different.
 *
 * Example 1:
 *   Input:  text = "aab"
 *   Output: ["aba"]
 *
 * Example 2:
 *   Input:  text = "aabb"
 *   Output: ["abab"]
 *
 * Example 3:
 *   Input:  text = "abccdd"
 *   Output: ["cdabcd"]
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function rearangeString(text: string): string[] {
  throw new Error('Not implemented');
}
