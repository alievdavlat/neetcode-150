/**
 * 1217. Find the first repeated character in a given   ·   Easy   ·   Everyday Functions
 *
 * Write a function to find the first repeated character in a given string.
 *
 * Example 1:
 *   Input:  text = "Google"
 *   Output: "o"
 *
 * Example 2:
 *   Input:  text = "data"
 *   Output: "a"
 *
 * Example 3:
 *   Input:  text = "python"
 *   Output: "0"
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function firstRepeatedChar(text: string): string {
  throw new Error('Not implemented');
}
