/**
 * 1207. Count the longest repeating subsequences such that the   ·   Easy   ·   Everyday Functions
 *
 * Write a function to count the longest repeating subsequences such that the
 * two subsequences don’t have same string characters at same positions.
 *
 * Example 1:
 *   Input:  text = "AABEBCDD"
 *   Output: 3
 *
 * Example 2:
 *   Input:  text = "aabb"
 *   Output: 2
 *
 * Example 3:
 *   Input:  text = "aab"
 *   Output: 1
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function findLongestRepeatingSubseq(text: string): number {
  throw new Error('Not implemented');
}
