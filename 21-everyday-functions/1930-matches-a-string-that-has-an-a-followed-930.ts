/**
 * 1930. Matches a string that has an a followed   ·   Easy   ·   Everyday Functions
 *
 * Write a function that matches a string that has an a followed by zero or
 * more b's by using regex.
 *
 * Example 1:
 *   Input:  text = "msb"
 *   Output: "Not matched!"
 *
 * Example 2:
 *   Input:  text = "a0c"
 *   Output: "Found a match!"
 *
 * Example 3:
 *   Input:  text = "abbc"
 *   Output: "Found a match!"
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function textMatch(text: string): string {
  throw new Error('Not implemented');
}
