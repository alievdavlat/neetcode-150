/**
 * 1073. Split the given string with multiple delimiters by   ·   Easy   ·   Everyday Functions
 *
 * Write a function to split the given string with multiple delimiters by using
 * regex.
 *
 * Example 1:
 *   Input:  text = "Forces of the \ndarkness*are coming into the play."
 *   Output: ["Forces of the ","darkness","are coming into the play."]
 *
 * Example 2:
 *   Input:  text = "Mi Box runs on the \n Latest android*which has google assistance and chromecast."
 *   Output: ["Mi Box runs on the "," Latest android","which has google assistance and chromecast."]
 *
 * Example 3:
 *   Input:  text = "Certain services\nare subjected to change*over the seperate subscriptions."
 *   Output: ["Certain services","are subjected to change","over the seperate subscriptions."]
 *
 * Pattern:   One idea, written out directly
 * Target:    Not stated for this set - aim for the obvious single pass
 * Source:    MBPP · Google Research · Apache 2.0
 */
export function multipleSplit(text: string): string[] {
  throw new Error('Not implemented');
}
