/**
 * 24. Generate Parentheses   ·   Medium   ·   Stack
 *
 * Given n pairs of parentheses, generate every well-formed combination. Order
 * does not matter.
 *
 * Example 1:
 *   Input:  n = 3
 *   Output: ["((()))", "(()())", "(())()", "()(())", "()()()"]
 *
 * Example 2:
 *   Input:  n = 1
 *   Output: ["()"]
 *
 * Constraints:
 *   - 1 <= n <= 8
 *
 * Follow-up: What are the two rules that make a partial string still
 * extendable?
 *
 * Pattern:   Backtracking with an open/close counter
 * Target:    O(4^n / sqrt(n)) time — the nth Catalan number
 * LeetCode:  https://leetcode.com/problems/generate-parentheses/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=19233s  (05:20:33)
 */
export function generateParenthesis(n: number): string[] {
  throw new Error('Not implemented');
}
