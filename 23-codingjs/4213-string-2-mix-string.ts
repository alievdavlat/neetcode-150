/**
 * 4213. Mix String   ·   Medium   ·   CodingJS
 *
 * Return the characters of a and b interleaved, taking the first of a, then
 * the first of b, then the second of each, and so on. Once one string runs
 * out, the remainder of the other is added at the end.
 *
 * Example 1:
 *   Input:  a = "abc", b = "xyz"
 *   Output: "axbycz"
 *
 * Example 2:
 *   Input:  a = "Hi", b = "There"
 *   Output: "HTihere"
 *
 * Example 3:
 *   Input:  a = "xxxx", b = "There"
 *   Output: "xTxhxexre"
 *
 * Example 4:
 *   Input:  a = "xxx", b = "X"
 *   Output: "xXxx"
 *
 * Example 5:
 *   Input:  a = "2/", b = "27 around"
 *   Output: "22/7 around"
 *
 * Example 6:
 *   Input:  a = "", b = "Hello"
 *   Output: "Hello"
 *
 * Pattern:   Walk the string with a window
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function mixString(a: string, b: string): string {
  throw new Error('Not implemented');
}
