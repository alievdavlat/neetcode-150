/**
 * 4177. First Two   ·   Easy   ·   CodingJS
 *
 * Return the first two characters of str, or all of str when it is shorter
 * than that, so a single character comes back alone and the empty string stays
 * empty.
 *
 * Example 1:
 *   Input:  str = "Hello"
 *   Output: "He"
 *
 * Example 2:
 *   Input:  str = "abcdefg"
 *   Output: "ab"
 *
 * Example 3:
 *   Input:  str = "ab"
 *   Output: "ab"
 *
 * Example 4:
 *   Input:  str = "a"
 *   Output: "a"
 *
 * Example 5:
 *   Input:  str = ""
 *   Output: ""
 *
 * Example 6:
 *   Input:  str = "kitten"
 *   Output: "ki"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function firstTwo(str: string): string {
  throw new Error('Not implemented');
}
