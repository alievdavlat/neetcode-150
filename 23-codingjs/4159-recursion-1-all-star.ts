/**
 * 4159. All Star   ·   Medium   ·   CodingJS
 *
 * Return a copy of str with a star put between each character and the one that
 * follows it. Solve this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "hello"
 *   Output: "h*e*l*l*o"
 *
 * Example 2:
 *   Input:  str = "abc"
 *   Output: "a*b*c"
 *
 * Example 3:
 *   Input:  str = "ab"
 *   Output: "a*b"
 *
 * Example 4:
 *   Input:  str = "a"
 *   Output: "a"
 *
 * Example 5:
 *   Input:  str = ""
 *   Output: ""
 *
 * Example 6:
 *   Input:  str = "3.14"
 *   Output: "3*.*1*4"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function allStar(str: string): string {
  throw new Error('Not implemented');
}
