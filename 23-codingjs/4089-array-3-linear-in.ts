/**
 * 4089. Linear In   ·   Medium   ·   CodingJS
 *
 * Given outer and inner, both sorted in non-decreasing order, return true when
 * every value in inner also appears somewhere in outer.
 *
 * Example 1:
 *   Input:  outer = [1,2,4,6], inner = [2,4]
 *   Output: true
 *
 * Example 2:
 *   Input:  outer = [1,2,4,6], inner = [2,3,4]
 *   Output: false
 *
 * Example 3:
 *   Input:  outer = [1,2,4,4,6], inner = [2,4]
 *   Output: true
 *
 * Example 4:
 *   Input:  outer = [2,2,4,4,6,6], inner = [2,4]
 *   Output: true
 *
 * Example 5:
 *   Input:  outer = [2,2,2,2,2], inner = [2,2]
 *   Output: true
 *
 * Example 6:
 *   Input:  outer = [2,2,2,2,2], inner = [2,4]
 *   Output: false
 *
 * Pattern:   Two indices, or a running best
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function linearIn(outer: number[], inner: number[]): boolean {
  throw new Error('Not implemented');
}
