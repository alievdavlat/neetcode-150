/**
 * 4167. Star Bit   ·   Medium   ·   CodingJS
 *
 * The string str holds a dash and, somewhere after it, a star. Return the
 * piece of str running from that dash to that star, keeping both of them and
 * dropping everything outside. Solve this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "xyz,-abc*123"
 *   Output: "-abc*"
 *
 * Example 2:
 *   Input:  str = "x,-hello*"
 *   Output: "-hello*"
 *
 * Example 3:
 *   Input:  str = ",-xy*1"
 *   Output: "-xy*"
 *
 * Example 4:
 *   Input:  str = "not really ,-possible*"
 *   Output: "-possible*"
 *
 * Example 5:
 *   Input:  str = ",-abc*"
 *   Output: "-abc*"
 *
 * Example 6:
 *   Input:  str = ",-abc*xyz"
 *   Output: "-abc*"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function starBit(str: string): string {
  throw new Error('Not implemented');
}
