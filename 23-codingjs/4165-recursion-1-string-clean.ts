/**
 * 4165. String Clean   ·   Medium   ·   CodingJS
 *
 * Return a copy of str where each stretch of one character repeated next to
 * itself has been cut down to a single copy of that character. Solve this with
 * recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "yyzzza"
 *   Output: "yza"
 *
 * Example 2:
 *   Input:  str = "abbbcdd"
 *   Output: "abcd"
 *
 * Example 3:
 *   Input:  str = "Hello"
 *   Output: "Helo"
 *
 * Example 4:
 *   Input:  str = "XXabcYY"
 *   Output: "XabcY"
 *
 * Example 5:
 *   Input:  str = "112ab445"
 *   Output: "12ab45"
 *
 * Example 6:
 *   Input:  str = "Hello Bookkeeper"
 *   Output: "Helo Bokeper"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function stringClean(str: string): string {
  throw new Error('Not implemented');
}
