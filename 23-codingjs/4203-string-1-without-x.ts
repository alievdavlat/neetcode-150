/**
 * 4203. Without X   ·   Easy   ·   CodingJS
 *
 * Return str with its first character removed when that character is x and its
 * last character removed when that character is x. Every other x stays where
 * it is.
 *
 * Example 1:
 *   Input:  str = "xHix"
 *   Output: "Hi"
 *
 * Example 2:
 *   Input:  str = "xHi"
 *   Output: "Hi"
 *
 * Example 3:
 *   Input:  str = "Hxix"
 *   Output: "Hxi"
 *
 * Example 4:
 *   Input:  str = "Hi"
 *   Output: "Hi"
 *
 * Example 5:
 *   Input:  str = "xxHi"
 *   Output: "xHi"
 *
 * Example 6:
 *   Input:  str = "Hix"
 *   Output: "Hi"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function withoutX(str: string): string {
  throw new Error('Not implemented');
}
