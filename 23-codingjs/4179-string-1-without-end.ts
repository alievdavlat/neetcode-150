/**
 * 4179. Without End   ·   Easy   ·   CodingJS
 *
 * Return str with its first and last characters dropped. str is at least two
 * characters long, so a str of exactly two characters gives the empty string.
 *
 * Example 1:
 *   Input:  str = "Hello"
 *   Output: "ell"
 *
 * Example 2:
 *   Input:  str = "java"
 *   Output: "av"
 *
 * Example 3:
 *   Input:  str = "coding"
 *   Output: "odin"
 *
 * Example 4:
 *   Input:  str = "code"
 *   Output: "od"
 *
 * Example 5:
 *   Input:  str = "ab"
 *   Output: ""
 *
 * Example 6:
 *   Input:  str = "Chocolate!"
 *   Output: "hocolate"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function withoutEnd(str: string): string {
  throw new Error('Not implemented');
}
