/**
 * 4161. End X   ·   Medium   ·   CodingJS
 *
 * Return a copy of str with every lowercase x moved behind all the other
 * characters, which keep their original order. Solve this with recursion
 * rather than a loop.
 *
 * Example 1:
 *   Input:  str = "xxre"
 *   Output: "rexx"
 *
 * Example 2:
 *   Input:  str = "xxhixx"
 *   Output: "hixxxx"
 *
 * Example 3:
 *   Input:  str = "xhixhix"
 *   Output: "hihixxx"
 *
 * Example 4:
 *   Input:  str = "hiy"
 *   Output: "hiy"
 *
 * Example 5:
 *   Input:  str = "h"
 *   Output: "h"
 *
 * Example 6:
 *   Input:  str = "x"
 *   Output: "x"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function endX(str: string): string {
  throw new Error('Not implemented');
}
