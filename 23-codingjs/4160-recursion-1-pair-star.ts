/**
 * 4160. Pair Star   ·   Medium   ·   CodingJS
 *
 * Return a copy of str in which a star is inserted wherever a character is
 * immediately repeated, so that the two equal characters end up apart. Solve
 * this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "hello"
 *   Output: "hel*lo"
 *
 * Example 2:
 *   Input:  str = "xxyy"
 *   Output: "x*xy*y"
 *
 * Example 3:
 *   Input:  str = "aaaa"
 *   Output: "a*a*a*a"
 *
 * Example 4:
 *   Input:  str = "aaab"
 *   Output: "a*a*ab"
 *
 * Example 5:
 *   Input:  str = "aa"
 *   Output: "a*a"
 *
 * Example 6:
 *   Input:  str = "a"
 *   Output: "a"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function pairStar(str: string): string {
  throw new Error('Not implemented');
}
