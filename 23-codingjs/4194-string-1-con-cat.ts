/**
 * 4194. Con Cat   ·   Easy   ·   CodingJS
 *
 * Return a joined to b, except that when the last character of a is the same
 * as the first character of b only one of the pair is kept. Either string may
 * be empty.
 *
 * Example 1:
 *   Input:  a = "abc", b = "cat"
 *   Output: "abcat"
 *
 * Example 2:
 *   Input:  a = "dog", b = "cat"
 *   Output: "dogcat"
 *
 * Example 3:
 *   Input:  a = "abc", b = ""
 *   Output: "abc"
 *
 * Example 4:
 *   Input:  a = "", b = "cat"
 *   Output: "cat"
 *
 * Example 5:
 *   Input:  a = "pig", b = "g"
 *   Output: "pig"
 *
 * Example 6:
 *   Input:  a = "pig", b = "doggy"
 *   Output: "pigdoggy"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function conCat(a: string, b: string): string {
  throw new Error('Not implemented');
}
