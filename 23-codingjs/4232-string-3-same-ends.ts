/**
 * 4232. Same Ends   ·   Medium   ·   CodingJS
 *
 * Return the longest prefix of string that also appears at the end of string,
 * with the two copies not overlapping. The answer is therefore at most half as
 * long as string, and it is empty when no such prefix exists.
 *
 * Example 1:
 *   Input:  string = "abXYab"
 *   Output: "ab"
 *
 * Example 2:
 *   Input:  string = "xx"
 *   Output: "x"
 *
 * Example 3:
 *   Input:  string = "xxx"
 *   Output: "x"
 *
 * Example 4:
 *   Input:  string = "xxxx"
 *   Output: "xx"
 *
 * Example 5:
 *   Input:  string = "javaXYZjava"
 *   Output: "java"
 *
 * Example 6:
 *   Input:  string = "javajava"
 *   Output: "java"
 *
 * Pattern:   Scan for a run, remember the best
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function sameEnds(string: string): string {
  throw new Error('Not implemented');
}
