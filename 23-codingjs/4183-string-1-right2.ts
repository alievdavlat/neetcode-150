/**
 * 4183. Right2   ·   Easy   ·   CodingJS
 *
 * Return str with its last two characters moved to the front. str is at least
 * two characters long, so a str of exactly two characters is unchanged.
 *
 * Example 1:
 *   Input:  str = "Hello"
 *   Output: "loHel"
 *
 * Example 2:
 *   Input:  str = "java"
 *   Output: "vaja"
 *
 * Example 3:
 *   Input:  str = "Hi"
 *   Output: "Hi"
 *
 * Example 4:
 *   Input:  str = "code"
 *   Output: "deco"
 *
 * Example 5:
 *   Input:  str = "cat"
 *   Output: "atc"
 *
 * Example 6:
 *   Input:  str = "12345"
 *   Output: "45123"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function right2(str: string): string {
  throw new Error('Not implemented');
}
