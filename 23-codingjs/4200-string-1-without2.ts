/**
 * 4200. Without2   ·   Easy   ·   CodingJS
 *
 * When the first two characters of str are the same pair as its last two,
 * return str without that leading pair; otherwise return str unchanged. A str
 * of exactly two characters matches itself and gives the empty string.
 *
 * Example 1:
 *   Input:  str = "HelloHe"
 *   Output: "lloHe"
 *
 * Example 2:
 *   Input:  str = "HelloHi"
 *   Output: "HelloHi"
 *
 * Example 3:
 *   Input:  str = "Hi"
 *   Output: ""
 *
 * Example 4:
 *   Input:  str = "Chocolate"
 *   Output: "Chocolate"
 *
 * Example 5:
 *   Input:  str = "xxx"
 *   Output: "x"
 *
 * Example 6:
 *   Input:  str = "xx"
 *   Output: ""
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function without2(str: string): string {
  throw new Error('Not implemented');
}
