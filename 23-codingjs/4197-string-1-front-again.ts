/**
 * 4197. Front Again   ·   Easy   ·   CodingJS
 *
 * Return true when the first two characters of str are also its last two. A
 * str of exactly two characters counts, since the two pairs are then the same
 * characters.
 *
 * Example 1:
 *   Input:  str = "edited"
 *   Output: true
 *
 * Example 2:
 *   Input:  str = "edit"
 *   Output: false
 *
 * Example 3:
 *   Input:  str = "ed"
 *   Output: true
 *
 * Example 4:
 *   Input:  str = "jj"
 *   Output: true
 *
 * Example 5:
 *   Input:  str = "jjj"
 *   Output: true
 *
 * Example 6:
 *   Input:  str = "jjjj"
 *   Output: true
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function frontAgain(str: string): boolean {
  throw new Error('Not implemented');
}
