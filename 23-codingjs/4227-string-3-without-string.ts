/**
 * 4227. Without String   ·   Medium   ·   CodingJS
 *
 * Given strings base and remove, return base with every occurrence of remove
 * deleted, comparing the two without regard to case. Matches are taken from
 * left to right and never overlap, and the characters that survive keep the
 * case they had in base.
 *
 * Example 1:
 *   Input:  base = "Hello there", remove = "llo"
 *   Output: "He there"
 *
 * Example 2:
 *   Input:  base = "Hello there", remove = "e"
 *   Output: "Hllo thr"
 *
 * Example 3:
 *   Input:  base = "Hello there", remove = "x"
 *   Output: "Hello there"
 *
 * Example 4:
 *   Input:  base = "This is a FISH", remove = "IS"
 *   Output: "Th a FH"
 *
 * Example 5:
 *   Input:  base = "THIS is a FISH", remove = "is"
 *   Output: "TH a FH"
 *
 * Example 6:
 *   Input:  base = "THIS is a FISH", remove = "iS"
 *   Output: "TH a FH"
 *
 * Pattern:   Scan for a run, remember the best
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function withoutString(base: string, remove: string): string {
  throw new Error('Not implemented');
}
