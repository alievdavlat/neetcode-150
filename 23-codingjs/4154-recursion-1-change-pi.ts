/**
 * 4154. Change Pi   ·   Medium   ·   CodingJS
 *
 * Return a copy of str in which every occurrence of the letters pi has been
 * swapped for the text 3.14. Solve this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "xpix"
 *   Output: "x3.14x"
 *
 * Example 2:
 *   Input:  str = "pipi"
 *   Output: "3.143.14"
 *
 * Example 3:
 *   Input:  str = "pip"
 *   Output: "3.14p"
 *
 * Example 4:
 *   Input:  str = "pi"
 *   Output: "3.14"
 *
 * Example 5:
 *   Input:  str = "hip"
 *   Output: "hip"
 *
 * Example 6:
 *   Input:  str = "p"
 *   Output: "p"
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function changePi(str: string): string {
  throw new Error('Not implemented');
}
