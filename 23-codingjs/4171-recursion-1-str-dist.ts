/**
 * 4171. Str Dist   ·   Medium   ·   CodingJS
 *
 * Given a non-empty sub, find the longest section of str that has sub at its
 * front and sub at its end, and return how many characters long it is, or 0
 * when sub is absent. Solve this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "catcowcat", sub = "cat"
 *   Output: 9
 *
 * Example 2:
 *   Input:  str = "catcowcat", sub = "cow"
 *   Output: 3
 *
 * Example 3:
 *   Input:  str = "cccatcowcatxx", sub = "cat"
 *   Output: 9
 *
 * Example 4:
 *   Input:  str = "abccatcowcatcatxyz", sub = "cat"
 *   Output: 12
 *
 * Example 5:
 *   Input:  str = "xyx", sub = "x"
 *   Output: 3
 *
 * Example 6:
 *   Input:  str = "xyx", sub = "y"
 *   Output: 1
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function strDist(str: string, sub: string): number {
  throw new Error('Not implemented');
}
