/**
 * 4170. Str Copies   ·   Medium   ·   CodingJS
 *
 * Given a non-empty sub and a non-negative n, return true when str holds at
 * least n occurrences of sub, where occurrences may overlap each other. Solve
 * this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "catcowcat", sub = "cat", n = 2
 *   Output: true
 *
 * Example 2:
 *   Input:  str = "catcowcat", sub = "cow", n = 2
 *   Output: false
 *
 * Example 3:
 *   Input:  str = "catcowcat", sub = "cow", n = 1
 *   Output: true
 *
 * Example 4:
 *   Input:  str = "iiijjj", sub = "i", n = 3
 *   Output: true
 *
 * Example 5:
 *   Input:  str = "iiijjj", sub = "i", n = 4
 *   Output: false
 *
 * Example 6:
 *   Input:  str = "iiijjj", sub = "ii", n = 2
 *   Output: true
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function strCopies(str: string, sub: string, n: number): boolean {
  throw new Error('Not implemented');
}
