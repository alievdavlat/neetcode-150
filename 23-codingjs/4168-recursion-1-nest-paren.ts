/**
 * 4168. Nest Paren   ·   Medium   ·   CodingJS
 *
 * Return true when str is empty, or is a run of opening parentheses followed
 * by the same number of closing parentheses and nothing else, so that every
 * pair wraps the next. Solve this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "(())"
 *   Output: true
 *
 * Example 2:
 *   Input:  str = "((()))"
 *   Output: true
 *
 * Example 3:
 *   Input:  str = "(((x))"
 *   Output: false
 *
 * Example 4:
 *   Input:  str = "((())"
 *   Output: false
 *
 * Example 5:
 *   Input:  str = "((()()"
 *   Output: false
 *
 * Example 6:
 *   Input:  str = "()"
 *   Output: true
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function nestParen(str: string): boolean {
  throw new Error('Not implemented');
}
