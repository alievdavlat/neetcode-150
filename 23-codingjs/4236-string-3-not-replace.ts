/**
 * 4236. Not Replace   ·   Medium   ·   CodingJS
 *
 * Given a string str, return it with every standalone lowercase is expanded to
 * is not. An occurrence is standalone only when the character directly before
 * it and the character directly after it are not alphabetic letters, so the is
 * inside a longer word is left alone.
 *
 * Example 1:
 *   Input:  str = "is test"
 *   Output: "is not test"
 *
 * Example 2:
 *   Input:  str = "is-is"
 *   Output: "is not-is not"
 *
 * Example 3:
 *   Input:  str = "This is right"
 *   Output: "This is not right"
 *
 * Example 4:
 *   Input:  str = "This is isabell"
 *   Output: "This is not isabell"
 *
 * Example 5:
 *   Input:  str = ""
 *   Output: ""
 *
 * Example 6:
 *   Input:  str = "is"
 *   Output: "is not"
 *
 * Pattern:   Scan for a run, remember the best
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function notReplace(str: string): string {
  throw new Error('Not implemented');
}
