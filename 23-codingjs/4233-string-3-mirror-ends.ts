/**
 * 4233. Mirror Ends   ·   Medium   ·   CodingJS
 *
 * Return the longest prefix of string whose reverse appears at the end of
 * string. The two parts are allowed to overlap, so a palindrome gives back the
 * whole of string, and the answer is empty when the first and last characters
 * differ.
 *
 * Example 1:
 *   Input:  string = "abXYZba"
 *   Output: "ab"
 *
 * Example 2:
 *   Input:  string = "abca"
 *   Output: "a"
 *
 * Example 3:
 *   Input:  string = "aba"
 *   Output: "aba"
 *
 * Example 4:
 *   Input:  string = "abab"
 *   Output: ""
 *
 * Example 5:
 *   Input:  string = "xxx"
 *   Output: "xxx"
 *
 * Example 6:
 *   Input:  string = "xxYxx"
 *   Output: "xxYxx"
 *
 * Pattern:   Scan for a run, remember the best
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function mirrorEnds(string: string): string {
  throw new Error('Not implemented');
}
