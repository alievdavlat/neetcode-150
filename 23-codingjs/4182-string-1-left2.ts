/**
 * 4182. Left2   ·   Easy   ·   CodingJS
 *
 * Return str with its first two characters moved to the end. str is at least
 * two characters long, so a str of exactly two characters is unchanged.
 *
 * Example 1:
 *   Input:  str = "Hello"
 *   Output: "lloHe"
 *
 * Example 2:
 *   Input:  str = "java"
 *   Output: "vaja"
 *
 * Example 3:
 *   Input:  str = "Hi"
 *   Output: "Hi"
 *
 * Example 4:
 *   Input:  str = "code"
 *   Output: "deco"
 *
 * Example 5:
 *   Input:  str = "cat"
 *   Output: "tca"
 *
 * Example 6:
 *   Input:  str = "12345"
 *   Output: "34512"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function left2(str: string): string {
  throw new Error('Not implemented');
}
