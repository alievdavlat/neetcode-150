/**
 * 4220. Same Star Char   ·   Medium   ·   CodingJS
 *
 * Return true when every star in str that has a character on both sides has
 * the same character on each side. Stars at the very front or back of str are
 * ignored, and an empty str gives true.
 *
 * Example 1:
 *   Input:  str = "xy*yzz"
 *   Output: true
 *
 * Example 2:
 *   Input:  str = "xy*zzz"
 *   Output: false
 *
 * Example 3:
 *   Input:  str = "*xa*az"
 *   Output: true
 *
 * Example 4:
 *   Input:  str = "*xa*bz"
 *   Output: false
 *
 * Example 5:
 *   Input:  str = "*xa*a*"
 *   Output: true
 *
 * Example 6:
 *   Input:  str = ""
 *   Output: true
 *
 * Pattern:   Walk the string with a window
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function sameStarChar(str: string): boolean {
  throw new Error('Not implemented');
}
