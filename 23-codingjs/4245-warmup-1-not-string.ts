/**
 * 4245. Not String   ·   Easy   ·   CodingJS
 *
 * Return str with 'not ' placed in front of it, unless str already begins with
 * 'not', in which case return str unchanged.
 *
 * Example 1:
 *   Input:  str = "candy"
 *   Output: "not candy"
 *
 * Example 2:
 *   Input:  str = "x"
 *   Output: "not x"
 *
 * Example 3:
 *   Input:  str = "not bad"
 *   Output: "not bad"
 *
 * Example 4:
 *   Input:  str = "bad"
 *   Output: "not bad"
 *
 * Example 5:
 *   Input:  str = "not"
 *   Output: "not"
 *
 * Example 6:
 *   Input:  str = "is not"
 *   Output: "not is not"
 *
 * Pattern:   One condition, written out plainly
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function notString(str: string): string {
  throw new Error('Not implemented');
}
