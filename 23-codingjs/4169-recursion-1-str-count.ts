/**
 * 4169. Str Count   ·   Medium   ·   CodingJS
 *
 * Given a non-empty sub, return how many times sub occurs in str when matches
 * are taken from left to right and no character is used by two matches. Solve
 * this with recursion rather than a loop.
 *
 * Example 1:
 *   Input:  str = "catcowcat", sub = "cat"
 *   Output: 2
 *
 * Example 2:
 *   Input:  str = "catcowcat", sub = "cow"
 *   Output: 1
 *
 * Example 3:
 *   Input:  str = "catcowcat", sub = "dog"
 *   Output: 0
 *
 * Example 4:
 *   Input:  str = "cacatcowcat", sub = "cat"
 *   Output: 2
 *
 * Example 5:
 *   Input:  str = "xyx", sub = "x"
 *   Output: 2
 *
 * Example 6:
 *   Input:  str = "iiiijj", sub = "i"
 *   Output: 4
 *
 * Pattern:   A base case and one step smaller
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function strCount(str: string, sub: string): number {
  throw new Error('Not implemented');
}
