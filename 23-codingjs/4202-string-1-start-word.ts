/**
 * 4202. Start Word   ·   Easy   ·   CodingJS
 *
 * Say that word matches str when the two run the same from the front, with the
 * very first character allowed to differ. Return the leading part of str that
 * is as long as word on a match, or the empty string when there is no match or
 * str is too short to hold word.
 *
 * Example 1:
 *   Input:  str = "hippo", word = "hi"
 *   Output: "hi"
 *
 * Example 2:
 *   Input:  str = "hippo", word = "xip"
 *   Output: "hip"
 *
 * Example 3:
 *   Input:  str = "hippo", word = "i"
 *   Output: "h"
 *
 * Example 4:
 *   Input:  str = "hippo", word = "ix"
 *   Output: ""
 *
 * Example 5:
 *   Input:  str = "h", word = "ix"
 *   Output: ""
 *
 * Example 6:
 *   Input:  str = "", word = "i"
 *   Output: ""
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function startWord(str: string, word: string): string {
  throw new Error('Not implemented');
}
