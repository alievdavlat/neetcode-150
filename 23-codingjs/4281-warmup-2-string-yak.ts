/**
 * 4281. String Yak   ·   Easy   ·   CodingJS
 *
 * Return str with every three-character run that starts with y and ends with k
 * removed, whatever the middle character happens to be. Such runs never
 * overlap.
 *
 * Example 1:
 *   Input:  str = "yakpak"
 *   Output: "pak"
 *
 * Example 2:
 *   Input:  str = "pakyak"
 *   Output: "pak"
 *
 * Example 3:
 *   Input:  str = "yak123ya"
 *   Output: "123ya"
 *
 * Example 4:
 *   Input:  str = "yak"
 *   Output: ""
 *
 * Example 5:
 *   Input:  str = "yakxxxyak"
 *   Output: "xxx"
 *
 * Example 6:
 *   Input:  str = "xxxyakyyyakzzz"
 *   Output: "xxxyyzzz"
 *
 * Pattern:   A loop over a string or array
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function stringYak(str: string): string {
  throw new Error('Not implemented');
}
