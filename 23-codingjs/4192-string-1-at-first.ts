/**
 * 4192. At First   ·   Easy   ·   CodingJS
 *
 * Return a two character string taken from the front of str, using @ in place
 * of each character str does not have, so an empty str gives @@.
 *
 * Example 1:
 *   Input:  str = "hello"
 *   Output: "he"
 *
 * Example 2:
 *   Input:  str = "hi"
 *   Output: "hi"
 *
 * Example 3:
 *   Input:  str = "h"
 *   Output: "h@"
 *
 * Example 4:
 *   Input:  str = ""
 *   Output: "@@"
 *
 * Example 5:
 *   Input:  str = "kitten"
 *   Output: "ki"
 *
 * Example 6:
 *   Input:  str = "java"
 *   Output: "ja"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function atFirst(str: string): string {
  throw new Error('Not implemented');
}
