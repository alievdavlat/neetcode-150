/**
 * 4180. Combo String   ·   Easy   ·   CodingJS
 *
 * Return the shorter of a and b, then the longer one, then the shorter one
 * again. The two lengths are never equal, though either string may be empty.
 *
 * Example 1:
 *   Input:  a = "Hello", b = "hi"
 *   Output: "hiHellohi"
 *
 * Example 2:
 *   Input:  a = "Hi", b = "Hello"
 *   Output: "HiHelloHi"
 *
 * Example 3:
 *   Input:  a = "aaa", b = "b"
 *   Output: "baaab"
 *
 * Example 4:
 *   Input:  a = "b", b = "aaa"
 *   Output: "baaab"
 *
 * Example 5:
 *   Input:  a = "aaa", b = ""
 *   Output: "aaa"
 *
 * Example 6:
 *   Input:  a = "", b = "bb"
 *   Output: "bb"
 *
 * Pattern:   Slice and join, minding the short cases
 * Target:    Not stated for this set
 * Source:    CodingJS, after Nick Parlante · statement written for this workspace
 */
export function comboString(a: string, b: string): string {
  throw new Error('Not implemented');
}
