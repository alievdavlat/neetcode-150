/**
 * 148. Missing Number   ·   Easy   ·   Bit Manipulation
 *
 * nums holds n distinct numbers taken from the range [0, n]. Return the one
 * number in that range that is missing.
 *
 * Example 1:
 *   Input:  nums = [3, 0, 1]
 *   Output: 2
 *
 * Example 2:
 *   Input:  nums = [0, 1]
 *   Output: 2
 *
 * Example 3:
 *   Input:  nums = [9, 6, 4, 2, 3, 5, 7, 0, 1]
 *   Output: 8
 *
 * Constraints:
 *   - n === nums.length and 1 <= n <= 10^4
 *   - 0 <= nums[i] <= n with all values distinct
 *
 * Follow-up: Two O(n) answers with O(1) space. One of them cannot overflow —
 * which?
 *
 * Pattern:   XOR of indices and values (or the Gauss sum)
 * Target:    O(n) time, O(1) space
 * LeetCode:  https://leetcode.com/problems/missing-number/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=137243s  (38:07:23)
 */
export function missingNumber(nums: number[]): number {
  throw new Error('Not implemented');
}
