/**
 * 107. Maximum Product Subarray   ·   Medium   ·   1-D Dynamic Programming
 *
 * Given an integer array nums, return the largest product of any contiguous
 * non-empty subarray. Every prefix and suffix product fits in a 32-bit
 * integer.
 *
 * Example 1:
 *   Input:  nums = [2, 3, -2, 4]
 *   Output: 6          // [2, 3]
 *
 * Example 2:
 *   Input:  nums = [-2, 0, -1]
 *   Output: 0
 *
 * Constraints:
 *   - 1 <= nums.length <= 2 * 10^4
 *   - -10 <= nums[i] <= 10
 *
 * Follow-up: A negative number swaps the roles of your two running values.
 *
 * Pattern:   Carry both the running max and the running min
 * Target:    O(n) time, O(1) space
 * LeetCode:  https://leetcode.com/problems/maximum-product-subarray/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=94223s  (26:10:23)
 */
export function maxProduct(nums: number[]): number {
  throw new Error('Not implemented');
}
