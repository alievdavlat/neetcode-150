/**
 * 104. Palindromic Substrings   ·   Medium   ·   1-D Dynamic Programming
 *
 * Count the palindromic substrings of s. Substrings at different positions
 * count separately even when they consist of the same characters.
 *
 * Example 1:
 *   Input:  s = "abc"
 *   Output: 3          // "a", "b", "c"
 *
 * Example 2:
 *   Input:  s = "aaa"
 *   Output: 6          // "a" x3, "aa" x2, "aaa"
 *
 * Constraints:
 *   - 1 <= s.length <= 1000
 *   - s consists of lowercase English letters
 *
 * Pattern:   Same expand-around-centre, counting instead of measuring
 * Target:    O(n^2) time, O(1) space
 * LeetCode:  https://leetcode.com/problems/palindromic-substrings/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=90664s  (25:11:04)
 */
export function countSubstrings(s: string): number {
  throw new Error('Not implemented');
}
