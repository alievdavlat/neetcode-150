/**
 * Scratch pad. Import whatever you are working on, log it, run `npm run play`.
 * Nothing here is ever graded — overwrite it as often as you like.
 */
import { containsDuplicate } from './01-arrays-hashing/001-contains-duplicate.ts';
import { buildList, listToArray } from './shared/testing.ts';

console.log(containsDuplicate([1, 2, 3, 1]));
console.log(listToArray(buildList([1, 2, 3])));
