/**
 * 3. Two Sum   ·   Easy   ·   Arrays & Hashing
 *
 * Given an array of integers nums and an integer target, return the indices of
 * the two numbers that add up to target. Exactly one valid answer exists, and
 * you may not reuse the same element twice. The order of the two returned
 * indices does not matter.
 *
 * Example 1:
 *   Input:  nums = [2, 7, 11, 15], target = 9
 *   Output: [0, 1]     // nums[0] + nums[1] === 9
 *
 * Example 2:
 *   Input:  nums = [3, 2, 4], target = 6
 *   Output: [1, 2]
 *
 * Example 3:
 *   Input:  nums = [3, 3], target = 6
 *   Output: [0, 1]
 *
 * Constraints:
 *   - 2 <= nums.length <= 10^4
 *   - -10^9 <= nums[i] <= 10^9
 *   - -10^9 <= target <= 10^9
 *   - Exactly one valid answer exists
 *
 * Follow-up: Can you do it in one pass instead of two?
 *
 * Pattern:   Hash map of value -> index, one pass
 * Target:    O(n) time, O(n) space
 * LeetCode:  https://leetcode.com/problems/two-sum/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=1110s  (00:18:30)
 */
export function twoSum(nums: number[], target: number): [number, number] {
  throw new Error('Not implemented');
}
