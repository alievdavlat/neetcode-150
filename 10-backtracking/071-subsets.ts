/**
 * 71. Subsets   ·   Medium   ·   Backtracking
 *
 * Given an array of unique integers, return the power set — every possible
 * subset. No subset may repeat, and the order of subsets does not matter.
 *
 * Example 1:
 *   Input:  nums = [1, 2, 3]
 *   Output: [[], [1], [2], [1,2], [3], [1,3], [2,3], [1,2,3]]
 *
 * Example 2:
 *   Input:  nums = [0]
 *   Output: [[], [0]]
 *
 * Constraints:
 *   - 1 <= nums.length <= 10
 *   - -10 <= nums[i] <= 10
 *   - All values are unique
 *
 * Follow-up: There is a bitmask version with no recursion at all.
 *
 * Pattern:   Include / exclude each element
 * Target:    O(n * 2^n) time, O(n) recursion depth
 * LeetCode:  https://leetcode.com/problems/subsets/
 * Video:     https://www.youtube.com/watch?v=T0u5nwSA0w0&t=58543s  (16:15:43)
 */
export function subsets(nums: number[]): number[][] {
  throw new Error('Not implemented');
}
